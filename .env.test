# Test environment variables
# This file is loaded when ENV=test

# Application Settings
QUOIN_ENV=test
QUOIN_LOG_LEVEL=DEBUG
QUOIN_OTEL_ENABLED=False

# Database Settings
QUOIN_POSTGRES_DB=test_db

# OAuth — left blank; tests use dependency_overrides (no real server needed)
QUOIN_OAUTH_JWKS_URI=
QUOIN_OAUTH_ISSUER=
QUOIN_OAUTH_AUDIENCE=
QUOIN_OAUTH_ROLES_CLAIM=roles
